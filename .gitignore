# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# Credentials
*.json
!terraform.tfvars.example

# K8s secrets (apply manually, don't commit real credentials)
k8s/**/secret.yaml

# OS
.DS_Store
