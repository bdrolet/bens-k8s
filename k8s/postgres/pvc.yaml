apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  # GKE Autopilot defaults to standard-rwo (SSD-backed PD).
  resources:
    requests:
      storage: 10Gi
